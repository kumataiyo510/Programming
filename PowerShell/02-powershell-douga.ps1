# 次回（https://www.youtube.com/watch?v=4wSmlg9lfO0）
# Windows Powershell and Powershell(crossplatform)

# どんなコマンドがあるか調べるとき
get-command

# コマンドの使い方を調べるとき
get-help

# 成果物のオブジェクトについて調べられる
get-member

# スイッチパラメータ：引数を引かない場合などに多い、仮に引数で$false $trueをつけてもON OFF可能
# パラメータはctrl + space で一覧を表示できる
# カンマでパラメータを複数指定可能なものがある1

# ${} 変数と明示的に指定可能
# $() 中身が式として扱われる